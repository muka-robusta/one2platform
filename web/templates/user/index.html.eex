<h2>Users list</h2>

<table class="table">
	<%= for user <- @users do %>
		<tr>
			<td><b><%= user.first_name %></b> (<%= user.id %>)</td>
			<td><%= link "View", to: user_path(@conn, :show, user.id) %></td>
		</tr>
		<% end %>
</table>